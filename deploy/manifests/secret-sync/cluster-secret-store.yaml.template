apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: budget-analyzer-oci-vault
spec:
  provider:
    oracle:
      principalType: InstancePrincipal
      region: "__OCI_REGION__"
      vault: "__OCI_VAULT_OCID__"
      compartment: "__OCI_COMPARTMENT_OCID__"
