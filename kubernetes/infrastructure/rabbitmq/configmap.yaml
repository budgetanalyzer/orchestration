apiVersion: v1
kind: ConfigMap
metadata:
  name: rabbitmq-config
  namespace: infrastructure
  labels:
    app: rabbitmq
data:
  rabbitmq.conf: |
    definitions.import_backend = local_filesystem
    definitions.local.path = /etc/rabbitmq/definitions/definitions.json
    definitions.skip_if_unchanged = true

    # TLS listener (disable plaintext AMQP)
    listeners.ssl.default = 5671
    listeners.tcp = none

    ssl_options.cacertfile = /tls-ca/ca.crt
    ssl_options.certfile = /tls/tls.crt
    ssl_options.keyfile = /tls/tls.key
    ssl_options.verify = verify_none
    ssl_options.fail_if_no_peer_cert = false

    # Logging
    log.console = true
    log.console.level = info

    # Prometheus is the intended metrics source; disable the management
    # agent's internal metrics collector using RabbitMQ's supported setting.
    management_agent.disable_metrics_collector = true
