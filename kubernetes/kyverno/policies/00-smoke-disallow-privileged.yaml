apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: smoke-disallow-privileged
  annotations:
    policies.kyverno.io/title: Smoke Disallow Privileged
    policies.kyverno.io/category: Platform Smoke
    policies.kyverno.io/severity: medium
    policies.kyverno.io/subject: Pod
    policies.kyverno.io/description: >-
      Retained scoped smoke policy used by the platform prerequisite verifier
      to prove that Kyverno admission is live before the full security
      guardrail policy suite runs.
spec:
  validationFailureAction: Enforce
  background: false
  rules:
    - name: disallow-privileged-in-smoke-namespaces
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaceSelector:
                matchLabels:
                  security.budgetanalyzer.io/kyverno-smoke: "true"
      validate:
        message: "Privileged pods are blocked by the Kyverno smoke policy."
        pattern:
          spec:
            containers:
              - securityContext:
                  =(privileged): false
