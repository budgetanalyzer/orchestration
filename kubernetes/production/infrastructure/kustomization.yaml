apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../infrastructure

patches:
  - path: patches/redis-storage.yaml
    target:
      group: apps
      version: v1
      kind: StatefulSet
      name: redis
