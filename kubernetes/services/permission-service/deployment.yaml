apiVersion: apps/v1
kind: Deployment
metadata:
  name: permission-service
  namespace: default
  labels:
    app: permission-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: permission-service
  template:
    metadata:
      labels:
        app: permission-service
    spec:
      serviceAccountName: permission-service
      containers:
      - name: permission-service
        image: permission-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8086
          name: http
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "kubernetes"
        # Database connection
        - name: SPRING_DATASOURCE_URL
          valueFrom:
            secretKeyRef:
              name: permission-service-postgresql-credentials
              key: url
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: permission-service-postgresql-credentials
              key: username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: permission-service-postgresql-credentials
              key: password
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        startupProbe:
          httpGet:
            path: /permission-service/actuator/health
            port: 8086
          periodSeconds: 5
          failureThreshold: 24  # 5s * 24 = 120s max startup time
        readinessProbe:
          httpGet:
            path: /permission-service/actuator/health
            port: 8086
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /permission-service/actuator/health
            port: 8086
          periodSeconds: 20
